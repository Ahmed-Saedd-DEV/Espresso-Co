{
  "name": "espresso-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix server run dev",
    "start": "npm --prefix server run start",
    "studio": "npm --prefix server run studio"
  },
  "devDependencies": {
    "jest": "^30.5.1"
  }
}
